import { add, greet } from '../src/<%= moduleName %>';

describe('<%= moduleName %>', () => {
  it('should add', () => {
    expect(add(2, 3)).toEqual(5);
  });
  it('should greet', () => {
    expect(greet('world')).toEqual('<%= name %> says: hello to world');
  });
});
