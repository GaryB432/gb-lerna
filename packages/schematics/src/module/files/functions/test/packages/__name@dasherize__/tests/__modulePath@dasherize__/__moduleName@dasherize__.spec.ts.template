import { add, greet } from '<%= srcPath %><%= dasherize(moduleName) %>';

describe('<%= moduleName %>', () => {
  test('adds', () => {
    expect(add(2, 3)).toEqual(5);
  });
  test('greets', () => {
    expect(greet('world')).toEqual('<%= moduleName %> says: hello to world');
  });
});
